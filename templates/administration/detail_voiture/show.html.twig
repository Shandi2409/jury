{% extends 'base.html.twig' %}

{% block title %}DetailVoiture{% endblock %}

{% block body %}
    <h1>DetailVoiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail_voiture.id }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ detail_voiture.couleur }}</td>
            </tr>
            <tr>
                <th>Modeles</th>
                <td>{{ detail_voiture.modeles }}</td>
            </tr>
            <tr>
                <th>Nombre_portes</th>
                <td>{{ detail_voiture.nombrePortes }}</td>
            </tr>
            <tr>
                <th>Type_carburant</th>
                <td>{{ detail_voiture.typeCarburant }}</td>
            </tr>
            <tr>
                <th>Boite_vitesse</th>
                <td>{{ detail_voiture.boiteVitesse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detail_voiture_index') }}">back to list</a>

    <a href="{{ path('detail_voiture_edit', {'id': detail_voiture.id}) }}">edit</a>

    {{ include('administration/detail_voiture/_delete_form.html.twig') }}
{% endblock %}
