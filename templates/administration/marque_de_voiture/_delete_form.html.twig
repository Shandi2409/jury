<form method="post" action="{{ path('marque_de_voiture_delete', {'id': marque_de_voiture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marque_de_voiture.id) }}">
    <button class="btn">Delete</button>
</form>
