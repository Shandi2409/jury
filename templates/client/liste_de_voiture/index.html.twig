{% extends 'base.html.twig' %}

{% block title %}MarqueDeVoiture index
{% endblock %}

{% block body %}
    <div class="row mt-3">
    <div class="col-12">
    <a href="{{ path('liste_marque') }}" class="btn btn-link ml-3"> Revenir à la liste </a>
    </div>
    {% for voiture in marque_de_voiture.detailVoitures %}
		<div class="col-12 col-md-4">
        <a href="{{ path('detail_voiture', {'id':voiture.id}) }}">
            <div class="card mt-4 mx-4">
                <div class="card-body text-center">
                    <img src="/images/{{voiture.images}}" class="ml-auto" style="height:100px; object-fit:contain;"/>
                    <p class="text-center">
                    {{ voiture.modeles }}
                    </p>
                </div>
            </div>
            </a>
        </div>		
								
            {% endfor %}
    </div>
			
{% endblock %}
